version: "2"
run:
  tests: false
  timeout: 10m
  build-tags:
    - codeanalysis

linters:
  default: none
  enable:
    - bodyclose
    - dogsled
    - goconst
    - govet
    - ineffassign
    - misspell
    - prealloc
    - staticcheck
    - unconvert
  disable:
    - errcheck
    - gosec   # since you only had an exclusion rule
    - unused  # optional, often bundled with staticcheck

formatters:
  enable:
    - gofmt
    - goimports
